apiVersion: v1
kind: Service
metadata:
  name: mcp-k8s
  namespace: mcp-k8s
  labels:
    app.kubernetes.io/name: mcp-k8s
    app.kubernetes.io/part-of: mcp-k8s
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-k8s
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
